# ask the user to input a string
mystr = input("enter a string: ")
# find the middle character of the string (round down if the string len is even)
mid=(len(mystr)-1)//2
# print out the middle character
print("the middle character is: "+mystr[mid])
